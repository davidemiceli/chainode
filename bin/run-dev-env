#!/bin/bash


# Start environment for development
echo -e "Starting dockerized environment for development."
cd docker-compose/
docker-compose -f network.yml -f kafka.yml -f mongodb.yml -f dev-app.yml $@
cd ../
